groups:
- name: ingress-alerts
  rules:
  - alert: IngressMemoryHigh
    expr: container_memory_working_set_bytes{pod=~"ingress-nginx-controller.*"} > 400000000
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: "Ingress controller memory above safe threshold"